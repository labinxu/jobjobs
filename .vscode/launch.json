{
  "version": "0.1.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch debug",
      "program": "${workspaceFolder}/src/index.ts",
      "preLaunchTask": "pnpm build",
      "dap-compilation": "pnpm build",
      "outFiles": ["${workspaceFolder}/dist/**/*.js"]
    }
  ]
}
